# Data Directory

This directory stores dataset documentation and versioned split files only.

Do not commit raw VisA images, extracted datasets, or downloaded archives.

Use `data/splits/` for train/validation/test CSVs generated by local analysis scripts.
